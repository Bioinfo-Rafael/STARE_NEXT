# STARE_NEXT

This repository now keeps the previous all-source exploratory screening under:

- `work/legacy_all_sources_screening/`

The current focused implementation lives under:

- `work/kp_dst_excel_scan/`

It scans Kp/Dst geomagnetic features against datasets listed in the earthquake/geodetic Excel registry. The pipeline is exploratory only and does not claim earthquake prediction or causality.
